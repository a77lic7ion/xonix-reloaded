// Liquid Blueprint reminder: the sole page is a mobile-first territory-capture instrument, not a conventional app shell.
import GameCanvas from "@/components/GameCanvas";

export default function App() {
  return <GameCanvas />;
}
